{
  "name": "connectify",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm --workspace apps/server run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/server run build && npm --workspace apps/web run build",
    "lint": "npm --workspace apps/server run lint && npm --workspace apps/web run lint",
    "typecheck": "npm --workspace apps/server run typecheck && npm --workspace apps/web run typecheck"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
